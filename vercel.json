{
  "version": 2,
  "framework": "vite",
  "buildCommand": "node -e \"console.log('Skipping TypeScript check...'); require('child_process').execSync('npm exec vite build', {stdio: 'inherit'});\"",
  "outputDirectory": "dist",
  "installCommand": "npm install",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
} 